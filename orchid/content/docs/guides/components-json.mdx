`components.json` tells the shadcn CLI where to write source files, which
stylesheet contains Tailwind, and how to resolve the Orchid registry.

## Recommended configuration

Keep the compatible values generated by `shadcn init`, then verify these
settings:

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "base-nova",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/styles.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "base-ui": "@/base-ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "registries": {
    "@orchid": "https://orchid-ui-hitpay.vercel.app/r/{name}.json"
  }
}
```

Use the actual global stylesheet path generated by your framework if it differs
from `src/styles.css`. Ensure `@/*` resolves to your source directory. Do not
set `aliases.ui`. Orchid uses `base-ui`, `components`, `utils`, `lib`, and
`hooks` only.

## File destinations

Orchid registry files declare their own target alias:

- `@/base-ui/actions/button.tsx` resolves through `aliases.base-ui` to
  `src/base-ui/actions/button.tsx`.
- `@/components/form/form-builder.tsx` resolves through `aliases.components` to
  `src/components/form/form-builder.tsx`.

Do not add a TypeScript path `@base-ui/*`. That would shadow the `@base-ui/react`
package. Use `@/base-ui/…` instead.

Do not manually choose a destination when adding an item. The registry target
and your aliases determine it consistently.

## Next step

Install the [Orchid theme tokens](/theming), then add a component from
[Installation](/installation).
