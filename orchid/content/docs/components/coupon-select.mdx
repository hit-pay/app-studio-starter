import { CouponSelectDemo } from '../../../src/components/doc/doc-demos'

<CouponSelectDemo />

Coupon dropdown. Loads `GET /v1/coupons`. Do not call `list-coupons` on the screen.

Persist `id` plus name snapshot. Optional: `multiple`.
