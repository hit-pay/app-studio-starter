import { InputGroupDemo } from '../../../src/components/doc/doc-demos'

Use `InputGroup` to combine an input with an adjacent prefix, suffix, or related control. Choose it when the parts form one value or action.
<InputGroupDemo />
