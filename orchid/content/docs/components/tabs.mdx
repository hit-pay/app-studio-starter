import { TabsDemo } from "../../../src/components/doc/doc-demos"

Use `Tabs` to switch between related views that share the same page context. Choose it for navigation among peer sections, not for submitting a form value.
<TabsDemo />
