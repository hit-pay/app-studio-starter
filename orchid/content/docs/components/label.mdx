import { LabelDemo } from '../../../src/components/doc/doc-demos'

Use `Label` to identify a form control or section. Choose it when composing a custom form control; use `Field` when you also need help or validation text.
<LabelDemo />
