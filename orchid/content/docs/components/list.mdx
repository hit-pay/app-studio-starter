import { ListDemo } from '../../../src/components/doc/doc-demos'

<ListDemo />

Prefer the `DataList` block for app lists. Import `@/components/displaying-data/data-list`
instead of composing these parts by hand.
