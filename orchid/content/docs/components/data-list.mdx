import { DataListDemo } from '../../../src/components/doc/doc-demos'

<DataListDemo />

Pass **`items` only**. Do not import row primitives. Do not
use `DataTable` unless the list needs search, filters, sort, or pagination.
Do not wrap `DataList`.

