import { ResizableDemo } from '../../../src/components/doc/demos/resizable-demo'

<ResizableDemo />

## Usage

```tsx
import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from '@/components/ui/resizable'

<ResizablePanelGroup orientation="horizontal">
  <ResizablePanel defaultSize={36} minSize={20}>
    Catalog
  </ResizablePanel>
  <ResizableHandle withHandle />
  <ResizablePanel defaultSize={64} minSize={30}>
    Detail
  </ResizablePanel>
</ResizablePanelGroup>
```

Give the group a bounded height. Use `orientation="vertical"` to stack panes.
