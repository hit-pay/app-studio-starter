import { InputDemo } from '../../../src/components/doc/doc-demos'

Use `Input` for a single-line text, numeric, email, or similar value. Choose it for ordinary text entry; use `Textarea` for multi-line content.
<InputDemo />
