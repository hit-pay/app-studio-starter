import { TaxSelectDemo } from '../../../src/components/doc/doc-demos'

Use `TaxSelect` when a form needs a HitPay tax. Choose it instead of building a tax dropdown or calling the list API from the screen.
<TaxSelectDemo />
