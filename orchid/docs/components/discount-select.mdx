import { DiscountSelectDemo } from '../doc-demos'

Use `DiscountSelect` when a form needs a discount from HitPay. Choose it instead of building a discount dropdown or calling the list API from the screen.
<DiscountSelectDemo />
