import { MetricCardDemo } from "../../src/docs/doc-demos"

Use `MetricCard` to highlight a key aggregate or KPI with its label and value. Choose it for dashboard summaries, not for detailed records or editable values.
<MetricCardDemo />
