import { useState } from "react"
import { DocExamples, DocPropsTable } from "../doc-code-panel"
import { CommandDemo } from "../ui/demos/command-demo"
import { Command } from "../../src/components/overlays/command"
import commandDocs, { COMMAND_EXAMPLE_GROUPS } from "./command"

Use `Command` for searchable command or option selection, especially keyboard-driven workflows. Choose it when users need to find an action quickly; use `Select` for a small, ordinary option list. Drive it with `open`, `onOpenChange`, and grouped `items`.

## Props

<DocPropsTable props={commandDocs.props} />

## Examples

<CommandDemo />

<DocExamples
  noInline
  examples={[commandDocs.examples[0]]}
  scope={{
    COMMAND_GROUPS: COMMAND_EXAMPLE_GROUPS.map((group) => ({
      ...group,
      items: group.items.map((item) => ({
        ...item,
        onSelect: () => undefined,
      })),
    })),
    Command,
    useState,
  }}
/>
