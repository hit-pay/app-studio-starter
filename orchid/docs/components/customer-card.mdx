import { CustomerCardDemo } from '../doc-demos'

Use `CustomerCard` to show a compact summary of one customer. Choose it for a selected or referenced customer; use `DetailCard` for a fuller read-only record.
<CustomerCardDemo />
