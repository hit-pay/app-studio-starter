import { RoleSelectDemo } from '../../src/docs/doc-demos'

Use `RoleSelect` when a form needs to choose HitPay business roles. Choose it instead of fetching roles directly in the screen.
<RoleSelectDemo />
