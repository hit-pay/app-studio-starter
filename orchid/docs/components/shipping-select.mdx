import { ShippingSelectDemo } from '../doc-demos'

Use `ShippingSelect` when a form needs a HitPay shipping method. Choose it instead of building a shipping dropdown or calling the list API from the screen.
<ShippingSelectDemo />
