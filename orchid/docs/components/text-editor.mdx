import { DocExamples, DocPropsTable } from "../doc-code-panel"
import { TextEditor } from "../../src/components/form/text-editor"
import textEditorDocs, {
  TEXT_EDITOR_HANDOVER_NOTE,
  TEXT_EDITOR_READONLY_NOTE,
} from "./text-editor"

Use `TextEditor` for formatted, rich text content. Choose it when users need formatting; use `Textarea` for plain multi-line text. Rich text for notes and handover, built with [Lexical](https://lexical.dev/) (Meta). Toolbar: bold, italic, underline, heading, lists. Persist `onValueChange` payloads as Lexical JSON.

## Props

<DocPropsTable props={textEditorDocs.props} />

<DocExamples
  examples={textEditorDocs.examples}
  scope={{
    HANDOVER_NOTE: TEXT_EDITOR_HANDOVER_NOTE,
    READONLY_NOTE: TEXT_EDITOR_READONLY_NOTE,
    TextEditor,
  }}
/>
