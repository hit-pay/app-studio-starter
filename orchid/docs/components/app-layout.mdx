import { DocExamples, DocPropsTable } from "../doc-code-panel"
import { AppLayout } from "../../src/components/layout/app-layout"
import { PageLayout } from "../../src/components/layout/page-layout"
import appLayoutDocs from "./app-layout"

Use `AppLayout` as the embedded HitPay App Studio frame around every dashboard route. Tab and sidebar items update selection automatically; pass `pages` or `sidebarPages` to swap main content, or control routing yourself with `activeNavigation`, `onNavigationChange`, and `children`.

## Props

<DocPropsTable props={appLayoutDocs.props} />

<DocExamples
  examples={appLayoutDocs.examples}
  scope={{
    AppLayout,
    PageLayout,
  }}
/>
