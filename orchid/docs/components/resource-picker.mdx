
Use `ResourcePicker` when users must add or select HitPay catalog records such as products, customers, orders, charges, invoices, or add-ons. Choose it instead of a page table or a one-off entity dropdown; wire category and location filters inside the picker when needed.
