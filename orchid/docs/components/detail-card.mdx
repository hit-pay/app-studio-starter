import { DetailCardDemo } from '../doc-demos'

Use `DetailCard` for a read-only key/value view of one record. Choose it for detail pages; use `DataList` or `DataTable` for collections and `FormBuilder` for editing.
<DetailCardDemo />
