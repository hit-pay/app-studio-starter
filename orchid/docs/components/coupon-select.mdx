import { CouponSelectDemo } from '../../src/docs/doc-demos'

Use `CouponSelect` when a form needs a coupon from HitPay. Choose it instead of building a coupon dropdown or calling the list API from the screen.
<CouponSelectDemo />
