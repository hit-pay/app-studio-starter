import { KbdDemo } from '../../src/docs/doc-demos'

Use `Kbd` to display a keyboard key or shortcut in help text and command hints. Choose it for documentation or discoverability, not as an interactive control.
<KbdDemo />
