import { DocExamples, DocPropsTable } from "../doc-code-panel"
import { Checkbox } from "../../src/ui/checkbox"
import { Input } from "../../src/ui/input"
import { Label } from "../../src/ui/label"
import labelDocs from "./label"

Use `Label` for accessible text tied to a control via `htmlFor`. For full form layout with description and errors, prefer `Field` and `FieldLabel`; use standalone `Label` for compact rows such as checkboxes.

## Props

<DocPropsTable props={labelDocs.props} />

<DocExamples
  examples={labelDocs.examples}
  scope={{
    Checkbox,
    Input,
    Label,
  }}
/>
