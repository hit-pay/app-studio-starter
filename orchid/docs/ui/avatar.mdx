import { AvatarDemo } from "../../src/docs/doc-demos"

Use `Avatar` to represent a person, team, or entity with an image or initials. Choose it for compact identity cues, not for full profile details.
<AvatarDemo />
