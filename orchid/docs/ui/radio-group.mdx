import { RadioGroupDemo } from '../../src/docs/doc-demos'

Use `RadioGroup` for mutually exclusive choices where exactly one option is selected. Choose it for a small visible set; use `Select` for longer lists.
<RadioGroupDemo />
