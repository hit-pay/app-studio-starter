import { DocExamples, DocPropsTable } from "../doc-code-panel"
import { Label } from "../../src/ui/label"
import { RadioGroup, RadioGroupItem } from "../../src/ui/radio-group"
import radioGroupDocs from "./radio-group"

Use `RadioGroup` when exactly one option must be selected from a short list. Pair `RadioGroupItem` with `Label` (or `Field`) for accessible labels; use `Checkbox` or `CheckboxGroup` when multiple selections are allowed.

## Props

<DocPropsTable props={radioGroupDocs.props} />

<DocExamples
  examples={radioGroupDocs.examples}
  scope={{
    Label,
    RadioGroup,
    RadioGroupItem,
  }}
/>
