import { DocExamples, DocPropsTable } from "../doc-code-panel"
import {
  InputGroup,
  InputGroupAddon,
  InputGroupButton,
  InputGroupInput,
  InputGroupSeparator,
  InputGroupText,
  InputGroupTextarea,
} from "../../src/ui/input-group"
import { MailRegular, SearchRegular } from "@mingcute/react/core-regular"
import inputGroupDocs from "./input-group"

Use `InputGroup` to compose an input or textarea with leading or trailing addons, text prefixes, separators, and inline buttons. Prefer `InputGroupInput` and `InputGroupTextarea` inside the group so focus and invalid styles stay aligned.

## Props

<DocPropsTable props={inputGroupDocs.props} />

<DocExamples
  examples={inputGroupDocs.examples}
  scope={{
    InputGroup,
    InputGroupAddon,
    InputGroupButton,
    InputGroupInput,
    InputGroupSeparator,
    InputGroupText,
    InputGroupTextarea,
    MailIcon: MailRegular,
    SearchIcon: SearchRegular,
  }}
/>
