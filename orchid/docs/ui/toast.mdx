import { DocExamples, DocPropsTable } from "../doc-code-panel"
import { Button } from "../../src/ui/button"
import { toast, Toaster } from "../../src/ui/toast"
import toastDocs from "./toast"

Use `Toast` for brief, transient feedback after an action, such as success or failure. Choose it for non-blocking notifications; use `Banner` for persistent page-level context.

Mount `Toaster` once in the app root, then call `toast.add({ title, description, type })`.

<Toaster />

## Props

<DocPropsTable props={toastDocs.props} />

<DocExamples
  examples={toastDocs.examples}
  scope={{
    Button,
    toast,
  }}
/>
