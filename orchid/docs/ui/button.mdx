import { ButtonDemo } from '../doc-demos'

Use `Button` for an explicit user action such as submit, save, or navigation. Choose it for actions, not for navigation lists or passive status.
<ButtonDemo />
