import { DropdownMenuDemo } from '../../src/docs/doc-demos'

Use `DropdownMenu` for a compact menu of related actions opened from a trigger. Choose it for actions, not for selecting a persistent form value.
<DropdownMenuDemo />
