import { SliderDemo } from "../doc-demos"

Use `Slider` when users choose a numeric value or range along a bounded continuum. Choose it when direct manipulation is useful; use `Input` when exact text entry is primary.
<SliderDemo />
